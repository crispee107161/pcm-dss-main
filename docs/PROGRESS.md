# MVP v2 Build Progress

Tracks the build order from `mvp.md` §7 (which supersedes the earlier §14 order). Update this file as each step lands — it's a tracker, not a spec; `mvp.md` stays the source of truth for requirements.

Legend: ✅ Done and verified · 🟡 Partial / legacy code needs rework · ⬜ Not started

## Build order

| # | Step | FR / ALG | Status | Notes |
|---|---|---|---|---|
| 1 | Auth + roles (3 roles) | FR-01–FR-03 | ✅ | `Role` enum reduced to `MARKETING_MANAGER` / `MARKETING_TEAM` / `BUSINESS_OWNER`; `middleware.ts` role-routes; seed users for all three (2026-08-12). |
| 2 | Ingestion | FR-04–FR-09, ALG-01–ALG-03 | ✅ | `detect.ts`/`validate-ads.ts`/`upsert-ads.ts` rewritten for the 93-column monthly export keyed on `Ad ID`; `ADS_DAILY_CSV` retired. All 12 monthly ads files ingested via `scripts/ingest-ads.ts`. |
| 3 | Repository + derived measures — **hard stop** | FR-10, FR-11, ALG-09 | ✅ | Reconciliation verified exactly: 746 ad rows, ₱901,196.96 total spend, ₱740,382.55 messaging spend (2026-08-12). |
| 4 | Content Library + manual categorisation | FR-13 | ✅ | S3 Content Library (`/dashboard/{marketing,owner}/content`) and S4 Categorisation Review (`/dashboard/{marketing,owner}/categorize`) built with full Owner/MM/Team permission grid; `category_pending` + propose/accept/reject workflow; role-gated and browser-verified (2026-08-12). |
| 5 | Dashboard | §4.1, plus FR-30 | ✅ | **5a (shell) done 2026-08-12**: single `DashboardOverview` component shared by both routes, role-gated Export, data-anchored period selector defaulting to last complete month, 5 KPI cards (median CPI+IQR and median organic engagement replace the old mean/ratio figures — verified against §8's n=187/₱21.39 reference), alerts strip, top/bottom-10 CPI tables (aggregated by `ad_id`, not name), recent uploads. Cut features (regression, What-If simulation) removed from both dashboards and both sidebars. `MARKETING_TEAM` redirect loop fixed (was locked out entirely). **5b (charts) done 2026-08-12**: `components/dashboard/DashboardCharts.tsx` adds CPI histogram + SVG box plot (period-scoped, same population as the median-CPI KPI), category-performance bar chart with median engagement + n labelled per bar, organic reach/views dual-axis trend (last 3 months, mirrors the ad-trend convention), and FR-30's visits/follows/ratio series (full uploaded history, with the required "not a conversion funnel" caption). All wired into `DashboardOverview.tsx`; `lib/data/dashboard.ts` extended with `cpiDistribution`, `categoryPerformance`, `postReachViewsTrend`, `pageFunnelTrend`. |
| 6 | FR-25 budget reallocation | FR-25 | ✅ | Owner-only `/dashboard/owner/budget-reallocation` (2026-08-12): `lib/stats/budget-reallocation.ts` ranks messaging ads (rows aggregated per Ad ID first) with spend ≥ a configurable threshold (dropdown: ₱300/500/1000, default ₱1,000) into four equal-size rank-based CPI quartiles; UI shows the quartile cards, a 0–100% reallocation slider comparing a portion of Q4's spend against Q1's rate, the full Q4 ad list, and the mandatory retrospective caption. 6/6 new unit tests + full suite (146/146) pass, `tsc --noEmit` clean, `npm run build` succeeds. **Verified against the live DB, exact match to mvp.md §4.5**: n=108; Q1 27/₱375,809.90/31,875/₱11.79, Q2 27/₱170,739.45/10,360/₱16.48, Q3 27/₱104,309.29/5,116/₱20.39, Q4 27/₱59,745.30/1,988/₱30.05; counterfactual +3,079 inquiries. |
| 7 | FR-26 ad set/campaign efficiency ranking | FR-26 | ✅ | New `lib/stats/ad-set-ranking.ts` + `/dashboard/owner/ad-set-ranking` (2026-08-12), Owner-only. Groups messaging ads by `Ad set ID`/`Campaign ID` (never name — the name-reuse trap), ad-level rows aggregated per Ad ID first; reports spend/inquiries/CPI/ad count per group, sorted CPI ascending, `< 3` ads flagged "low confidence" not hidden; tabs switch between Ad Set and Campaign views; confounding caveat (different periods/targeting) shown on screen. The pre-existing `campaign-rankings` page (individual top-10 ads by volume/efficiency) is a different, valid feature — left as-is, just relabelled "Top Ads" in the nav to stop colliding with this page's name. 7/7 new unit tests + full suite (153/153) pass, `tsc --noEmit` clean, `npm run build` succeeds. **Live-verified**: total spend across all groups reconciles exactly to ₱740,382.55 (the messaging-spend reference figure, mvp.md §8); 17 distinct messaging ad sets/campaigns (expected subset of the dataset's 26 total ad sets — FR-26 scopes to messaging ads only). |
| 8 | Performance screens | FR-17, plus FR-29 | ✅ | **FR-29** (2026-08-12): `lib/stats/post-type-performance.ts` groups organic posts by `post_type`, medians (not sum-then-divide — this is a per-post distribution comparison, not a ratio) reach/engagement rate/views per group; new `/dashboard/{owner,marketing}/post-type-performance` (Owner + MM View/Full, MARKETING_TEAM view added to the marketing sidebar's `TEAM_VISIBLE_HREFS` — this is their screen per mvp.md §3); low-confidence flag at n<3, single-Links-post footnote, mandatory algorithmic-distribution confounding caveat on screen. 4 new unit tests, full suite 157/157, `tsc --noEmit` clean, `npm run build` succeeds. **Live-verified 2026-08-12 (later session)**: engagement-rate units check out exactly (DB stores 0–100 scale; live 0.59 ≡ mvp.md's documented 0.0059 fraction). Post counts do **not** match mvp.md §4.5's table (730 posts: Photos 331/Videos 328/Reels 70/Links 1) — live DB has 916 posts (Photos 430/Videos 337/Reels 147/Links 1/Text 1). Root cause: `UploadLog` shows a batch of monthly organic-post files (Jan–Jul 2026 + re-uploads) landed 2026-08-09, three days before mvp.md's table was written 2026-08-12 — that table carries the handoff's original illustrative figures, not a fresh computation, so it never picked up the expansion. Also a new `Text` post type (n=1) not in the spec's table. Code is correct and reflects live data; `mvp.md` is the requirements source of truth and was left untouched — flagging here so the discrepancy is known before defence, not treated as a bug. **FR-17** (aggregation "by advertisement" was broken): found and fixed a regression in `lib/stats/campaign-rankings.ts` — its `isDailyGranularity` filter (a leftover from the pre-rework daily-export era) compared `reporting_ends − reporting_starts < 1 day`, but every `Ad` row is now one ad's one calendar month (93-column monthly export, §6), so the filter matched zero rows and silently emptied every "Top Ads" volume/efficiency table on both `owner/campaign-rankings` and `marketing/campaign-rankings`. Filter removed from the ranking functions and both pages (kept exported only because `lib/stats/laggedCorrelation.ts`, unused cut-regression-era code left in place per mvp.md §5, still imports it); "by ad set/campaign" (FR-26, step 7) and "by month" (dashboard trend charts, step 5) already covered FR-17's other aggregation levels. Also fixed a real ALG-09 violation found in passing: `owner/category-performance`'s "by category" aggregation was averaging each post's own `engagement_rate` (mean of per-row rates) instead of `SUM(reactions+comments+shares)/SUM(reach)` (sum-then-divide) — corrected, with a `MethodologyNote` added explaining why. |
| 9 | Categorisation algorithms | FR-12, FR-14, ALG-04, ALG-05 | ✅ | **ALG-04 done 2026-08-12** (see prior entry — unchanged). **ALG-05 completed and live-verified (2026-08-13)**: `GROQ_API_KEY` rotated to a working key (the old value was a 7-character placeholder, not a real key — confirmed via a direct Groq smoke test before rotating). Ran `runLlmClassification`'s logic end-to-end against the live DB (all 916 posts missing `category_llm`, 62 batches of 15, `llama-3.1-8b-instant`, `temperature=0`) via a one-off CLI runner mirroring the action exactly minus the `auth()`/session wrapper (not kept — deleted after the run). **Result: 916/916 classified, 0 UNCLASSIFIED, all 62 `LlmClassificationRun` rows `succeeded=true`** — Groq's free-tier per-minute quota required ~12-15s pacing between batches (auto-retried on every 429), total run time ~15 minutes. |
| 10 | Method evaluation | FR-15, ALG-06 | ✅ | (2026-08-13) New `lib/stats/agreement.ts` — unweighted Cohen's kappa (`(po−pe)/(1−pe)`), percentage agreement, and a full 5×5 confusion matrix with `UNCLASSIFIED` as a genuine fifth label (not dropped/merged), Landis & Koch (1977) magnitude bands. 6 new unit tests (perfect agreement, chance-level agreement, `UNCLASSIFIED` handling, empty-sample guard, full matrix coverage, band boundaries). New `/dashboard/{owner,marketing}/method-evaluation` (S8 — Owner View, Marketing Manager Full, Marketing Team hidden per mvp.md §3) via `lib/data/method-evaluation.ts`, comparing `category_keyword` and `category_llm` against `category_final` on whatever posts currently carry a final label. 229/229 tests pass, `tsc --noEmit` clean, `npm run build` succeeds (48 routes). **Live-verified against the real DB post-ALG-05 (2026-08-13): n=738 (posts with `category_final` set), keyword kappa=0.633 (substantial, 95.9% agreement), LLM kappa=0.060 (slight, 58.1% agreement).** **Significant finding, surfaced on both screens as a red "Circularity warning" banner (not hidden):** 95.9% of `category_final` values are byte-identical to `category_keyword` — the same figure as the keyword method's agreement percentage. This means `category_final` for most of this sample was populated by bulk-accepting keyword suggestions (the S4 "Auto-Categorize" + "Accept all pending" flow) rather than independent human judgment, so comparing the keyword method against a reference substantially derived from itself structurally inflates its kappa and is **not** the honest, independent comparison FR-15 requires — the low LLM kappa is a symptom of this, not necessarily a real quality gap. The page computes and displays `keywordFinalMatchShare` live and warns whenever it exceeds 80%, rather than reporting the raw numbers without context. **Superseded same-day (2026-08-13, later session): in-app labelling queue replaced with an external ground-truth CSV pipeline.** The original `ManualLabelSample` in-app queue (previous paragraph) was fully deleted — migration `20260813090941_ground_truth_source_and_inter_coder_reliability` dropped the table and its 180-row sample, and the 7 files/dirs of that subsystem (model, `lib/stats/manual-sample.ts`, `lib/data/manual-label-sample.ts`, `actions/manual-label-sample.ts`, the `/dashboard/marketing/method-evaluation/sample` page, etc.) were removed. Root cause: the in-app queue lacked a category codebook, and once one existed the team decided labelling should happen externally against `CODEBOOK_content_categories.md` (two independent coders + `compute_kappa.py`, per `Developer_Note_Ground_Truth_Labelling.md` — both team-maintained docs, not in this repo) rather than through a blind in-app UI. Replaced with: `category_final_source` (`CategoryFinalSource?` enum incl. `MANUAL_GROUND_TRUTH`) + `category_final_assigned_by_id`/`_assigned_at` added to `FacebookPost`; `lib/ground-truth/label-map.ts` maps the codebook's lowercase snake_case labels (`product_showcase`, `promotional_offer`, `testimonial`, `entertainment`, `unclear`) to `CategoryLabel`, with `UNCLEAR` as a genuine 5th enum value distinct from the system's own `UNCLASSIFIED`; `scripts/import-ground-truth.ts` (usage: `npx tsx scripts/import-ground-truth.ts <path-to-csv>`) takes the team's `post_id, category` handoff CSV, all-or-nothing validates every row (bad category label or unresolvable `post_id` aborts before any write, so a bad row can't partially contaminate ground truth), resolves `post_id` against either `FacebookPost.id` or its external `post_id` string field, and stamps `category_final`/`category_final_source=MANUAL_GROUND_TRUTH`; `scripts/import-inter-coder-reliability.ts` (usage: `npx tsx scripts/import-inter-coder-reliability.ts <n> <percentAgreement> <kappa> [notes]`) records the two-coder inter-rater kappa from `compute_kappa.py`'s external report into the new `InterCoderReliability` model, read back as the human "ceiling" kappa via `getInterCoderReliability()`. `lib/data/method-evaluation.ts`'s new `loadGroundTruthMethodEvaluation()` compares `category_keyword`/`category_llm` only against posts where `category_final_source = MANUAL_GROUND_TRUTH` — the actual honest, non-circular comparison FR-15 requires, distinct from the pre-existing `loadMethodEvaluation()` (kept as a secondary diagnostic on how much of the library the S4 queue has finalised, not the study itself). **2026-08-13 (later, same day) verification**: confirmed no stale references to the deleted subsystem remain anywhere in the codebase (`category_manual`, `ManualLabelSample`, `requestManualLabelSample` all grep-clean outside a single explanatory code comment); both import scripts, the schema fields, and `loadGroundTruthMethodEvaluation()` line up correctly — the pipeline is built and ready to receive the team's ground-truth CSV. **Completed 2026-08-13 (later, same day)**: Jerome/Janine's external ground-truth pass landed and was imported — `docs/ground_truth_categories.csv` (200 rows) via `import-ground-truth.ts`, inter-coder numbers via `import-inter-coder-reliability.ts`. **Live-verified in-browser** (owner login, `/dashboard/owner/method-evaluation`): human inter-coder ceiling κ=0.650 (substantial, 78.5% agreement, n=200); keyword method vs. ground truth κ=0.139 (slight, 52.5% agreement); LLM method vs. ground truth κ=0.444 (moderate, 64.5% agreement). Notably this **flips** the circular `category_final`-based comparison (which showed keyword κ=0.351 beating LLM κ=0.193) — once compared against honest ground truth, the LLM method is actually the stronger classifier, exactly the kind of distortion the circularity warning predicted. FR-15 is now fully satisfied — no more open blockers on this step. |
| 11 | Analysis screen | FR-19–FR-22, ALG-07, ALG-08 | ✅ | (2026-08-12) New `/dashboard/{owner,marketing}/analysis` (S7 — Owner Full, MM Full, Team View), replacing the legacy "Correlation" nav item (unlinked, route file left on disk). **ALG-07/FR-19**: `lib/stats/ranking-comparison.ts` — Spearman rank correlation between Views and organic engagement rate, average-rank ties, top-10%/20% overlap; the 1 post with blank Views is excluded explicitly. **FR-20**: `lib/stats/category-distribution.ts` — median Views/engagement rate per `category_final`, uncategorised posts shown as their own row. **ALG-08/FR-21**: `lib/stats/correlation-selection.ts` — Shapiro-Wilk on `ad_engagement_rate`/`cost_per_inquiry` (n=187, matches mvp.md exactly) decides Pearson vs. Spearman at runtime, never computes both; persisted every run to new `CorrelationAssumptionRun` model (migration `20260812162643_add_correlation_assumption_run`). **FR-22**: `lib/stats/interpret.ts` — magnitude-banded wording (negligible/weak/moderate/strong), always paired with coefficient + n + p. New numerical foundation in `lib/stats/normal-dist.ts` (erf/normalCdf, AS-241 `normalQuantile`, Lanczos `lgamma`, incomplete-beta `studentTPValue`) and `lib/stats/shapiro-wilk.ts` (Royston 1995/AS R94 port) — validated against `scipy.stats.shapiro` (Python 1.17.1) to within 1e-8 on 5 reference cases including n=187 and both branch boundaries (n=5, n=12), not hand-derived or memorized. **Schema fix found and fixed in passing**: `FacebookPost.views` was `Int @default(0)`, silently coercing genuinely-blank Views cells to 0 — this directly contradicted ALG-07's explicit "exclude the 1 post with blank Views, never coerce to 0" edge case. Migrated to `Int?`; `validate-posts.ts` now uses `parseIntOrNull`; backfilled the 1 known blank-Views post (`scripts/backfill-null-views.ts`, kept). **Known gap**: raw source files for Apr–Jul 2025 (186 of 916 posts) no longer exist under `data/`, so that subset couldn't be re-verified for additional blanks — the 730-post subset that could be checked matches mvp.md's original "1 blank Views" figure exactly, which is reassuring but not proof. **Live-verified**: `tsc --noEmit` clean, full suite 209/209, `npm run build` succeeds (both new routes compile); ran against the live DB — FR-21's n=187 reproduces mvp.md exactly, and the predicted Spearman selection (mvp.md: "Y has skew ≈ 3.9, so Spearman will be selected") holds (`shapiroY.p ≈ 0`, method = SPEARMAN). **FR-28 (watch-through rate) deferred to a follow-up same-day session, then built** — see the row below. |
| — | FR-28 watch-through rate | FR-28 | ✅ | (2026-08-12) Added to `/dashboard/{owner,marketing}/post-type-performance` (S6, per mvp.md's Screens table §3 — not S7, despite the build-order list's "plus FR-28" on step 11, which reads as a documentation-order artifact). `lib/stats/watch-through.ts`: rate = Average Seconds viewed / Duration (sec), display-capped at 100% (raw >100% is a replay/loop artifact per mvp.md §4.8's risk table), correlation with engagement rate computed live (Spearman, via the same rank/p-value machinery as FR-19/FR-21) rather than hardcoding mvp.md's illustrative ρ=0.363. New `WatchThroughCard` component. **Schema fix in passing**: `duration_sec`/`avg_seconds_viewed` existed on `FacebookPost` since the schema rework but `validate-posts.ts` never parsed them — added `Duration (sec)` (guarded >0) and `Average Seconds viewed` parsing, backfilled all 12 available raw-file months via the real ingestion pipeline (`scripts/backfill-watch-through.ts`, kept; 401 rows updated). Same known gap as the FR-19 Views backfill: Apr–Jul 2025 source files no longer exist under `data/`. 6 new unit tests, full suite 214/214, `tsc --noEmit` clean, `npm run build` succeeds. **Live-verified against the live DB — matches mvp.md almost exactly**: n=397 (exact match to "computable for 397/730 posts"), rho=0.3626 (mvp.md: ρ=0.363), p=8.9e-14 (mvp.md: p<0.001), exactly 1 outlier post (matches the risk table's "footnote the 1 outlier post"). |
| 12 | FR-27 lifecycle diagnostics | FR-27 | ✅ | (2026-08-13) Added to `/dashboard/owner/analysis` only — Owner-facing per mvp.md §4.5, unlike the rest of S7 which both Owner and MM see; `AnalysisView` takes an optional `lifecycle` prop, populated only by the owner route. `lib/stats/ad-lifecycle.ts`: `month_of_life(ad, row) = row's calendar month index − the ad's own earliest month index` (per-ad origin, not calendar-wide); cohort curves restricted to ads whose max month-of-life reached each threshold (default 2 and 3 months) before being shown, guarding the survivorship-bias risk mvp.md flags; CPI at each point is sum-then-divide (ALG-09), null (not 0) when a month has zero results. Also computes the single-month-vs-4+-months overall CPI comparison and the Frequency (Impressions/Reach) vs. CPI diagnostic. **Bug found and fixed during live-verification**: the frequency diagnostic initially used Facebook's raw per-row `cost_per_result` column across *all* ads regardless of objective — for non-messaging ads (e.g. Reach-optimised) that's cost-per-reach, not cost-per-inquiry, an incomparable unit mixed in with messaging CPI. This flipped the correlation's sign (rho=+0.51 on n=706) versus mvp.md's documented ρ=−0.241. Fixed to restrict to messaging-ads rows only (`total_messaging_contacts > 0`) and derive CPI as spend/messaging-contacts per row, consistent with every other CPI figure in the app. 9 new unit tests (including one that specifically locks in the messaging-rows-only filter), full suite 223/223, `tsc --noEmit` clean, `npm run build` succeeds. **Live-verified — near-exact reproduction after the fix**: frequency diagnostic n=482 (exact match to mvp.md), median frequency 1.545 (mvp.md: 1.55), rho=−0.2411 (mvp.md: −0.241), p=8.3e-8 (mvp.md: p<0.001). Cohort curve CPI values are close but not identical to mvp.md's table (e.g. ≥2mo cohort: 15.66→15.37→13.64→13.10 here vs. mvp.md's 15.53→15.29→13.58→13.03) — same monotonic-decreasing pattern and n=187 messaging-ad population total, but per-cohort ad counts differ slightly (n=123/59 here vs. mvp.md's 114/50); most likely the same kind of reference-table staleness documented for FR-19/FR-29 rather than a code issue, given the frequency diagnostic (built from the same underlying ad rows) reproduces almost exactly. |
| 13 | Reports + audit log | FR-23, FR-24 | ✅ | (2026-08-13) **FR-24 audit trail**: new `CategoryAuditLog` model (migration `20260813042726_add_category_audit_log`) records every manual category decision — PROPOSE/ACCEPT/REJECT/OVERRIDE/BULK_ACCEPT — with user, timestamp, and previous/new category; `actions/categorize.ts` instrumented at all five call sites (deliberately excludes ALG-04's `autoCategorizeAll`, an automated suggestion pass, not a manual assignment). New `lib/data/audit-log.ts` merges `CategoryAuditLog` with the pre-existing `UploadLog` into one chronological timeline; new S11 screen at `/dashboard/{owner,marketing}/audit-log` (Owner Full, Marketing Manager View, Marketing Team hidden per mvp.md §3 access matrix — enforced both server-side and via nav filtering). **FR-23 reports rebuild**: `lib/reports/report-data.ts` rewritten from scratch — dropped the three cut features it previously depended on (regression, Holt-Winters forecast, lagged correlation; `lib/stats/regression.ts`/`forecast.ts`/`laggedCorrelation.ts` left on disk per mvp.md §5, just unwired) and replaced them with the app's current live analytics: dashboard KPI summary (`getDashboardOverview(...,...,all=true)`), FR-25 budget reallocation, FR-26 ad-set ranking, FR-28/29 post-type + watch-through, FR-19-22 analysis (ranking comparison, category distribution, correlation + `interpretCorrelation` wording), FR-27 lifecycle (owner-only, matching S7's own owner-only scoping), and FR-30 follows-per-100-visits with the mandatory "not a conversion funnel" caption. `ReportView.tsx` rewritten to match; mandatory captions/caveats from mvp.md §4.8's language table included on every new section (retrospective framing for budget reallocation, confounding caveats for ad-set/post-type, survivorship-bias note for lifecycle). **CSV export** added (`lib/reports/csv.ts` + `/api/reports/[role]/csv`) alongside the pre-existing PDF export — both gated to Owner + Marketing Manager only; Marketing Team gets View access to the Reports screen (added to `TEAM_VISIBLE_HREFS`) but no export buttons and a 403 on both export routes if hit directly. 223/223 tests pass, `tsc --noEmit` clean, `npm run build` succeeds (44 routes, including the two new `/audit-log` pages and `/api/reports/[role]/csv`). **Live-verified against the live DB** via a one-off script (not kept): owner report builds with budget reallocation n=108 (exact match to mvp.md's ₱1,000-threshold reference), 17 ad-set rows (matches step 7's reference), watch-through n=397 (exact match to FR-28's reference), lifecycle populated for the owner role and correctly `null` for the marketing role; CSV export produces a well-formed multi-block file. FR-30's live ratio (2.61 follows/100 visits) differs from mvp.md's documented 2.92 — confirmed via direct query to be the same "reference-table staleness" pattern already documented for FR-19/FR-27/FR-29 (579 `PageMetricDaily` rows now vs. the ~365-day/12-month span the reference figure was computed from), not a code defect. |
| 14 | FR-31 regression analysis (reinstated 2026-08-17/18, after being cut in mvp.md §5 on 2026-08-12) | FR-31 | ✅ | (2026-08-20) `docs/raven/FR31_Regression_Specification.md`, amended by `docs/raven/FR31_Amendment_TypeScript_Implementation.md` (TS-native, no Python service; Jarque-Bera + corroborating Shapiro-Wilk in place of Shapiro-Wilk alone) and `docs/raven/FR31_Answers_HC3_and_Reach.md` (HC3 p-values referred to the standard normal not t; reach summed not maxed across months, confirmed against the platform's own reported monthly frequency). New numerical core: `lib/stats/ols-core.ts` (equilibrated Modified Gram-Schmidt QR OLS — not a port of the cut `regression.ts`'s Gauss-Jordan solver — giving `(XᵀX)⁻¹` and the HC3 hat diagonal directly), `lib/stats/seeded-random.ts` (mulberry32 + sklearn-fold-size-matching 10-fold CV, explicitly documented as non-byte-identical to Python's `KFold`), `lib/stats/normal-dist.ts` extended with `fPValue`/`chiSquareUpperTailEvenDf`/`normalTwoTailedPValue` (all built on the already-existing `regularizedIncompleteBeta` and `shapiroWilk` — two of the amendment's "hardest routines to build" already existed in this repo from FR-21/ALG-08), and `lib/stats/fr31-regression.ts` (dataset build, VIF, Breusch-Pagan, Jarque-Bera, CV, residual diagnostic, two-specification comparison). New `RegressionRun` model (migration `20260820194912_add_regression_run`) persists one row per specification per S7 load, mirroring `CorrelationAssumptionRun`'s audit-trail pattern — written for Chapter 4, never read back by the app. New `components/analytics/RegressionSection.tsx` renders on both `/dashboard/{owner,marketing}/analysis` (S7), following spec §7's mandated order (specification → diagnostics → coefficients → accuracy → residual diagnostic); `lib/stats/budget-reallocation.ts` gained an exported `MIN_SPEND_THRESHOLD_PHP` constant, now shared by FR-25 and FR-31 rather than duplicated as private literals. 291/291 tests pass (62 new, including hard-pinned reproductions of every published reference number), `tsc --noEmit` clean, `npm run build` succeeds. **Live-verified against the live DB, exact match to the amendment's §7 acceptance table**: n=108/187 reproduce exactly; R²=0.548/0.398, VIF 1.35/1.16/1.29/1.10, Breusch-Pagan LM=9.29 p=0.0543, Jarque-Bera JB≈106.0, all coefficients and both OLS/HC3 SE sets and p-values match to 3–4 decimals; the specification-comparison logic independently re-derives the spec's own stability finding (`engagement_rate` not robust, `cpm`/`ctr` stable) without it being hardcoded. Cross-validated accuracy lands within the amendment's own shuffle-dependent tolerance (CV MAE ₱4.19 vs. published ₱4.15, MAPE 19.3% vs. 19.1%, CV R²=0.380 vs. 0.397 — expected, since `mulberry32` cannot byte-match Python's `KFold(shuffle=True, random_state=42)`, only the fold-size rule was matched). `scripts/fr31-dump.ts` (usage: `npx tsx scripts/fr31-dump.ts`) produces the raw Chapter 4 numbers per spec §10. One real bug found and fixed during live verification: the accuracy panel's in-sample R² was initially recomputed from back-transformed CPI-scale residuals (giving 0.460) instead of using the model's own R² on the fitted ln(cpi) scale (0.548, the spec's actual figure) — MAE/RMSE/MAPE were unaffected since those are correctly CPI-scale in both readings. **PR review + fixes (2026-08-20/21, `docs/raven/pr-review.md`)**: `code-review-analyst` found 3 HIGH, 8 MEDIUM, and several LOW issues in the uncommitted diff (none were math bugs — display gaps, error containment, test coverage, doc-path staleness, naming/a11y nits); all fixed except one deliberately deferred (audit-row retention policy, a real product/ops decision not invented unprompted). A follow-up review pass on the fixes themselves then caught one inaccurate code comment (H3's live-DB check only covered one direction of the filter equivalence claim — corrected to describe the actual, immaterial asymmetry found). Final count: 294/294 tests pass (3 more than the original 291, added during the fix pass), `tsc --noEmit` clean, `npm run build` succeeds. |

## Definition of done (mvp.md §8)

- [x] Ingesting all 12 ads files reconciles to ₱901,196.96 total spend / ₱740,382.55 messaging spend, 746 ad rows *(2026-08-13: ads figures re-confirmed unchanged. Organic + page-level series also checked this session — summing every first-time `ins=` across all 35 `POSTS_CSV` upload attempts gives exactly 916, matching the live `FacebookPost` count; `FollowerHistory`/`PageViewers` both show 2 non-overlapping 60-row batches (120 total each), consistent; the 5 `PAGE_METRIC_CSV` failures are all genuinely unsupported file types (`Top cities`, `Viewers` demographic breakdown, `Age & gender`) correctly rejected with specific errors, not silently dropped or a validator bug.)*
- [x] CPI reference figures reproduce: n=187, median ₱21.39 (all messaging); n=148/131/108 at ₱300/500/1000 thresholds *(2026-08-13 re-check: n=187 exact, median ₱21.50 vs ₱21.39 — reference-table staleness, not a bug. ₱500/₱1000 thresholds exact match (n=131/108). ₱300 threshold: n=155 vs 148 — larger gap than the other two, but checked the spend distribution near the boundary directly (17 ads spread smoothly through ₱250–350, no clustering/duplicate values at the cutoff) and found no filter bug; ₱300 sits in a denser part of the spend distribution than the other two thresholds, so the same handful of ads added since mvp.md's reference table was written shifts n more visibly there.)*
- [x] Organic engagement rate reproduces median ≈0.0069 across all 730 posts *(2026-08-13 re-check: live n=916 (post count grew, same known staleness as FR-29's 730→916 figure), raw median 0.7041 on the DB's 0–100 scale ≡ 0.007041 as a fraction — matches mvp.md's ≈0.0069 closely, consistent with staleness not a defect.)*
- [x] FR-15: three category columns stored permanently + Cohen's kappa/confusion matrix displayed *(built and live-verified — see step 10 above; ground-truth pass landed and imported 2026-08-13, real non-circular kappa now populated: keyword κ=0.139, LLM κ=0.444, human ceiling κ=0.650)*
- [x] FR-21: Shapiro-Wilk decides Pearson vs. Spearman at runtime *(built and live-verified — see step 11 above)*
- [x] FR-25 filtered quartile table + counterfactual + mandatory caption *(built, unit-tested, and reproduces the reference figures exactly against the live DB — see step 6 above)*
- [x] FR-27 month-of-life cohort curves built and live-verified; `ADS_DAILY_CSV` retired *(✅ retired, ✅ built — see step 12 above. The curves show the same decreasing-CPI pattern and correct n=187 total, and the "alongside" frequency diagnostic reproduces mvp.md almost exactly (n=482, rho=−0.2411 vs. −0.241) — but the cohort curve's own per-threshold ad counts (n=123/59 here vs. mvp.md's n=114/50) don't match exactly, most likely reference-table staleness rather than a code bug, flagged not hidden)*
- [x] FR-30 labelled "follows per 100 page visits" nowhere reads as "funnel"/"conversion rate" *(2026-08-13: grepped all user-visible labels across DashboardCharts/DashboardOverview/ReportView/csv.ts — every screen label reads "Follows per 100 (Page) Visits"; "funnel" only appears in internal names (`PageFunnelChart`, `funnelTotals`) and in the caveat text that explicitly disclaims it ("This is not a funnel or a conversion rate"), never as a mislabel.)*
- [x] FR-26 groups by `Ad set ID`/`Campaign ID`, not by name
- [x] Every analytical output on S7 shows coefficient + n + significance + magnitude together *(FR-22, `lib/stats/interpret.ts`)*
- [x] Every role sees only its permitted screens in the sidebar; server-side role check on every route *(2026-08-13 full re-audit: every `page.tsx` under `app/dashboard/{marketing,owner}` has an `auth()` + role check — no unguarded pages found, including the unlinked legacy `correlation`/`regression`/`simulation` routes (all MARKETING_MANAGER/BUSINESS_OWNER only, so still safe even though unreachable from nav). Both `/api/reports/[role]/{csv,pdf}` routes 403 `MARKETING_TEAM` and cross-check the requested `[role]` path segment against the caller's own role, preventing a Marketing Manager from requesting the Owner's export.)*
- [x] Every upload attempt is logged to the audit trail regardless of success or failure *(uploads via `UploadLog`, manual category assignments via the new `CategoryAuditLog` — both surfaced on S11 Audit Log, see step 13 above)*
- [x] Invalid or malformed CSV files show specific error messages, not crashes
- [x] Re-uploading the same month updates rather than duplicates
