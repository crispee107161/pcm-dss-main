# Audit of All docs/raven Files Dated 2026-09-05

Date: 5 September 2026
Scope: every file in `docs/raven/` with a Sep 5, 2026 filesystem mtime (excluding this audit and the companion `Sep4_Files_Audit_and_Verification_2026-09-05.md`), verified against current code/git state.

## File inventory (chronological by mtime)

| # | File | mtime |
|---|---|---|
| 1 | `FR31_Regression_Specification.md` | 00:46 |
| 2 | `Analysis_Screen_Landed_Open_Questions_2026-09-05.md` | 00:51 |
| 3 | `Budget_Reallocation_Review.md`* | 01:17 (*content dated "3 September 2026") |
| 4 | `FR08_707_Expansion_Response_2026-09-05.md` | 05:45 |
| 5 | `FR08_707_Figures_to_Reconcile.md` | 05:51 |
| 6 | `FR08_707_Figures_Reconciled_2026-09-05.md` | 06:05 |

## Verified status

| File | What it requested/reported | Verified current status | Evidence |
|---|---|---|---|
| `FR31_Regression_Specification.md` | Amended spec: r=0.958 (not 0.984) is correct for the n=108 population; residual-table display rule amended to "flagged rows by default, full list behind toggle" | ✅ Confirmed — both amendments present at `FR31_Regression_Specification.md:58,205,207-208,218,250` and match the code (`AnalysisView.tsx`/`ResidualDiagnosticTable.tsx`, per the Sep-4 audit) | Direct file read this session; cross-checked against Sep-4 audit's code verification |
| `Analysis_Screen_Landed_Open_Questions_2026-09-05.md` | Reports Analysis screen fully implemented (492 tests), not committed at time of writing; raises 5 confirmation questions (spec-as-truth acknowledgement, 2 stale r=0.984 citations in older memos, which cohort the headline quotes, residual-diagnostic disclosure exception, bare em-dash table placeholders) | Implementation: ✅ now committed (`f8c8522`, 2026-09-05 00:53 — postdates this memo's 00:51 write time). **All 5 questions: 🔴 unanswered** — no reply memo exists in `docs/raven/` addressing any of them | `git log`; grep across `docs/raven/` for a reply found none |
| `Budget_Reallocation_Review.md` | §0.1/0.2 em dash + plain language; §2 slider default 50%/neutral color; §3 asks whether FR-11/FR-12 use the fixed threshold constant regardless of this screen's selector; §4 plain-language finding; §5 collapse Q4 table to 10 | §2, §4, §5: ✅ verified implemented in `components/analytics/BudgetReallocation.tsx` (DEFAULT_REALLOCATION_PCT=50, neutral color at line ~124, Q4_TABLE_COLLAPSED_COUNT=10) and `app/dashboard/owner/budget-reallocation/page.tsx` (finding sentence). §0.1: **corrected 2026-09-05** — the original claim of "no em dashes found in either file" overstated a clean grep; both files do contain the em dash character (`page.tsx:52`; `BudgetReallocation.tsx:15,21,64,136`), including one used as a literal `'—'` placeholder for a zero-value quartile (line 64). None of these are in user-facing prose/copy — they're either code comments or the "not applicable" table-cell convention §0.1 is about UI *copy*, which this project treats separately (see the parallel bare-em-dash question raised and left unresolved in `Analysis_Screen_Landed_Open_Questions_2026-09-05.md` §5) — so §0.1 likely still holds as intended, but the audit's own claim was imprecise as written. §3: ✅ **CLOSED** — answered in writing via `Budget_Reallocation_Threshold_Independence_Confirmed_2026-09-05.md`: `fr31-regression.ts:150,799` always uses the fixed `FR31_MIN_SPEND_PHP` constant, the Analysis/S7 page has no `minSpend`/`searchParams` handling at all, and the residual diagnostic inherits the same fixed-threshold fit (`lib/data/analysis.ts:211-212`) — so FR-11 and FR-12 are fully independent of the Budget Reallocation dropdown. §3's Definition-of-Terms rewording remains manuscript text outside this repo, not independently verifiable here | Code read this session (`fr31-regression.ts`, `lib/data/analysis.ts:211-212,799`, `BudgetReallocation.tsx`, `budget-reallocation/page.tsx`, `app/dashboard/owner/analysis/`); commit `7e1bc1f`; reply memo `Budget_Reallocation_Threshold_Independence_Confirmed_2026-09-05.md` |
| `FR08_707_Expansion_Response_2026-09-05.md` | Implements FR-08 scoring at n=707 alongside the existing n=200, per-category recall, ceiling-banner scoping; not committed | ✅ Consistent with current `git status` (all 5 named files — `page.tsx`, `MethodAgreementCard.tsx`, `method-evaluation.ts`, `agreement.test.ts`, `agreement.ts`, plus new `method-evaluation.test.ts` — still show modified/untracked, confirming "not committed yet" is still accurate as of this audit) | `git status` at session start |
| `FR08_707_Figures_to_Reconcile.md` | Raises 4 blocking/near-blocking questions: which model produced the 200 vs 507 `category_llm` values; which keyword kappa (0.136/0.212/0.1388) is current and whether the UNCLASSIFIED→unclear mapping applies; asks for a pooled inter-coder ceiling; confirms 719−12=707 | Fully answered by the next file (#6) | — |
| `FR08_707_Figures_Reconciled_2026-09-05.md` | §1: 704/707 posts are `llama-3.1-8b-instant`, 3 posts re-run under `gpt-oss-20b` but all predicted UNCLASSIFIED both times (no figure impact) — not contaminated. §2: 0.1388 (live 93-keyword lexicon, unmapped) is current; UNCLASSIFIED→unclear mapping is confirmed NOT applied anywhere in the live app. §3: pooled-ceiling read path built (`getInterCoderReliabilityRows()`), waiting on the reviewer to send the pooled n/agreement/kappa. §4: 719−12=707 gap confirmed exactly (10 MANUAL_OVERRIDE + 2 ACCEPTED_SUGGESTION, pre-dating the legacy nulling) | ✅ `getInterCoderReliabilityRows()` confirmed present in `lib/data/method-evaluation.ts:93` (replacing the old singular `getInterCoderReliability()`), matching the memo's described change. §3 (pooled ceiling) is 🟡 **built but not populated** — banner will only render once the reviewer supplies the pooled figure; this is correctly an external dependency, not a code gap. Not committed — same uncommitted-file set as #4, confirmed via `git status` | Code read (`lib/data/method-evaluation.ts:93`); `git status` |

## Open items from the Sep 5 batch

1. **`Analysis_Screen_Landed_Open_Questions_2026-09-05.md`'s 5 confirmation questions are entirely unanswered** — no reply memo exists. None are described as blocking a commit (the memo says so explicitly), but per the memo's own priority order, §4 (residual-diagnostic disclosure exception) is flagged as "easiest to get wrong reading the screen cold" and should get an answer soonest.
2. ~~`Budget_Reallocation_Review.md` §3 needs a reply memo~~ — **CLOSED 2026-09-05** via `Budget_Reallocation_Threshold_Independence_Confirmed_2026-09-05.md`. FR-11 and FR-12 are confirmed independent of the Budget Reallocation dropdown, in writing and in code.
3. **`FR08_707_Figures_Reconciled_2026-09-05.md` §3 (pooled inter-coder ceiling) is waiting on the reviewer** to send the pooled n/agreement/kappa figures — code side is done and requires no further changes once that data arrives.
4. **The entire FR-08 707 work (5 modified files + 1 new test file, per `git status`) remains uncommitted**, consistent with both FR-08 memos explicitly saying "not committed yet, pending your read." This is intentional per the project's stated review-before-commit workflow, not an oversight.
5. **Definition of Terms rewording** for the ₱1,000 threshold ("set at" → "used in the reported analysis, permits alternatives") is manuscript text outside this repo — cannot be verified here; flagging so it isn't lost.

**`Budget_Reallocation_Review.md` is now closed** in its entirety except for the manuscript-text item (#5 above, outside this repo). The one remaining code-side gap in that file — the non-messaging-spend CPI bug — belongs to a different memo thread (the Sep 4 audit) and is tracked there, not as part of this review's own action items.

## Cross-reference with the Sep 4 audit

The Sep 4 audit's one open finding (Budget Reallocation's non-messaging-spend CPI filter, matching the bug already fixed in Top Ads/Ad-Set Ranking) is a **different, still-unaddressed issue** from `Budget_Reallocation_Review.md` §3 in this batch — §3 concerns whether the *threshold* is shared between screens, not whether *non-messaging months* are excluded from the spend numerator. Both are open, independent gaps in the same file (`lib/stats/budget-reallocation.ts` / `budget-reallocation/page.tsx`). Recommend fixing them together rather than in separate passes, since they touch the same `aggregateByAdId`/`prisma.ad.findMany` code path.
