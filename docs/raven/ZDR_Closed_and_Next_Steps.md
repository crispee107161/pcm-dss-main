# ZDR closed. Screenshot is sufficient, and one small process note.

**Date:** 4 September 2026
**Re:** `Account_Items_Reply_2026-09-04.md`, second version
**Status:** one item closed, one answer, one request, one process note

---

## 1. ZDR is closed

Global Zero Data Retention enabled org-wide, with the global toggle overriding the per-API settings. That is the right configuration, and it means the inference endpoints are covered without needing to reason about which sub-setting applies to which call.

**Two things follow immediately.**

The Second Supplementary Agreement and Disclosure can now go to the client. Its statement that the Developers enabled Zero Data Retention is true as of today, which it was not yesterday.

The Chapter 3 paragraph loses its conditional framing. The third sentence stands as written.

---

## 2. The screenshot is sufficient. No corroboration needed.

You flagged that the Groq Console does not surface a timestamp on that page.

That is fine. Nobody is going to challenge whether a setting was enabled, and what the appendix needs is evidence that it is on rather than proof of the minute it was switched. A support export would be more than the claim requires.

We will record the date ourselves in the appendix caption, along the lines of "Groq Console, Data Controls, captured 4 September 2026." Our record of when we received it is adequate.

- [ ] **Please send `docs/raven/zdr-ss.jpg` directly** rather than leaving it in the repository, since the appendix is assembled outside it

---

## 3. §1.1 answered from our side

You listed confirming whether you are one of the five named signatories on the Non-Disclosure Agreement as an open item for you.

That is ours to answer rather than yours to look up, and it should not have been on your list. We will confirm it against the signed document.

- [ ] Consider it closed on your end

---

## 4. Still open, and none of it blocks us

- **§1.2**, the post-defence account arrangement and the current tier or billing. Needed before the manuscript is finished rather than this week.
- **§2**, key scoping and the current rate limits.

On the rate limits, the reason it matters is demonstration planning rather than the appendix. A full classification run is roughly 49 calls in quick succession, and if that would approach a limit we would rather demonstrate a small run than have a cooldown appear while a panel is watching. Knowing the number lets us decide in advance.

---

## 5. One process note

This memo arrived as a second file with the same name, the same date, and the same opening and closing sections as the first. The new §1 sits between them.

It read as a duplicate at first glance and was nearly skipped.

- [ ] **When revising a memo, bump the filename or the date**, for example `Account_Items_Reply_2026-09-04-v2.md`, or lead with a line naming what changed

Not a criticism, and the content was the item we most wanted. Worth doing because we are now working through several parallel threads and a memo that looks like one already read is easy to lose.

---

## 6. What we need next, unrelated to Groq

The backlog coding export from `Backlog_Coding_Export_Request.md` is the long pole on our side. Two evenings of coding for two people, and it cannot begin until the CSV exists.

- [ ] **The export in §1 of that memo**, post identifier and caption only
- [ ] **The import format answers in §4**, so we code into the right shape from the start
- [ ] **Whoever is working the queue asked to stop**, per §2.1

Everything on the Groq list runs in parallel with that and none of it holds us up.
